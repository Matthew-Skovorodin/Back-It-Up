Matthew Skovorodin
Back it up!

Program backs up files into local ".backup" folder. Restores files with "-r" command.

To make:

    make

To clean:

    make clean

To run:

    ./BackItUp      (to backup)
    ./BackItUp -r   (to restore)
